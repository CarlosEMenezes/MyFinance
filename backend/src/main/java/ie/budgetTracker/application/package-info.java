/**
 * Use-case services, ports (interfaces), DTOs and mappers.
 *
 * <p>The only layer permitted to reach into {@code domain}. Depends inward on
 * the domain and outward on nothing — infrastructure supplies implementations
 * of the ports declared here (spec §4).
 */
package ie.budgetTracker.application;
