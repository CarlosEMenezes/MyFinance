# The hexagonal packages are created empty in Foundation and fill up as
# features land. Without this, ArchUnit fails a rule that matched no classes,
# so the architecture tests could not be written before the code they govern.
# Each rule starts enforcing the moment the first class arrives in its package.
archRule.failOnEmptyShould=false
